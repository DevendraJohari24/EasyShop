// import {createReducer, createAction} from  "@reduxjs/toolkit";

// export const increment = createAction('product/create');

// const productReducer = createReducer(initialState, (builder) => {
//     builder.addCase(increment, )
// })